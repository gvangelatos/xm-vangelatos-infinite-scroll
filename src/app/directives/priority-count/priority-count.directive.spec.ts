import { PriorityCountDirective } from './priority-count.directive';

describe('PriorityCountDirective', () => {
  it('should create an instance', () => {
    const directive = new PriorityCountDirective();
    expect(directive).toBeTruthy();
  });
});
